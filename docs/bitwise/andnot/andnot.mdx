---
title: AND NOT (&^)
---

import CodeBlock from "@theme/CodeBlock";

import AndNot from "!!raw-loader!./andnot.go";
import AndNotOut from "!!raw-loader!./andnotOut.txt";

The `&^` operator in Go, known as the "bit clear" operator, is used to selectively clear bits in the left operand based on the bits set to `1` in the right operand.
The bit clear operator (&^) is specific to Go

<CodeBlock language="go">{AndNot}</CodeBlock>

Output:

<CodeBlock language="text">{AndNotOut}</CodeBlock>
